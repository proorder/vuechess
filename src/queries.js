const host = "http://localhost:8000";

export const AUTH = host + "/api/auth";
export const MODEL = host + "/api/model";
export const REQUEST = host + "/api/request";
export const TOP_TRAINERS = host + "/api/top-trainers";
export const TRAINERS = host + "/api/trainers";
export const PRODUCTS = host + "/api/products";
export const REGISTRATION = host + "/api/registration";
